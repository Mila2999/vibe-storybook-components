import MarkdownRender from '../markdownRender';
import { Controls, Canvas, Meta } from '@storybook/blocks';
import * as Markdown from './markdown.stories';

<Meta of={Markdown} />

# Markdown

## Overview

The Markdown component is used to Markdown between pages.....

It can be used as a replacement for `a` tag.

<MarkdownRender>
## Title

dsf

</MarkdownRender>

<Canvas of={Markdown.Overview} />

## Props

<Controls />
